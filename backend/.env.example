# Database
# Connection string para PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://fixitnow:fixitnow123@localhost:5432/fixitnow?schema=public

# JWT Authentication
# Secret key para firmar tokens JWT (cambiar en producción)
JWT_SECRET=your-secret-key-change-in-production
# Tiempo de expiración del token (ejemplos: 1d, 7d, 24h)
JWT_EXPIRES_IN=7d

# Application
PORT=8000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
